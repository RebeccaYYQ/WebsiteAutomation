Plan
    For now, just do everything automatically. If you don't have an item then skip
    Add adjustable features later

To do
    a feature to add is stuff to get more affection (since some quests have that)